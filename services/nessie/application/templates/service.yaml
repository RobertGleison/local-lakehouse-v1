apiVersion: v1
kind: Service
metadata:
  name: nessie
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: nessie  # routes traffic to pods with this label (matches deployment.yaml)
  ports:
    - name: api
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
