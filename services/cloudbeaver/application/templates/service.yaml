apiVersion: v1
kind: Service
metadata:
  name: cloudbeaver
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: cloudbeaver  # routes traffic to pods with this label (matches deployment.yaml)
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
