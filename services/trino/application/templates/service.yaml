apiVersion: v1
kind: Service
metadata:
  name: trino
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: trino  # routes traffic to pods with this label (matches deployment.yaml)
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
