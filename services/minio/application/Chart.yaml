apiVersion: v2
name: minio
description: MinIO object storage — provides S3-compatible API for Bronze/Silver/Gold buckets
type: application
version: 0.1.0      # chart version — bump when you change the chart
appVersion: "latest" # version of MinIO itself
