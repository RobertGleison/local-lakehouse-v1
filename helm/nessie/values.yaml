# helm/nessie/values.yaml
versionStoreType: ROCKSDB

rocksdb:
  dbPath: /nessie/db

persistence:
  enabled: true
  storageClassName: local-path
  size: 2Gi

resources:
  requests:
    memory: 256Mi
    cpu: 100m
  limits:
    memory: 512Mi
    cpu: 500m

service:
  port: 19120
